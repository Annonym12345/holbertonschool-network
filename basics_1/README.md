───────┬──────────────────────────────────────────────────────────────────────────────────────────────
       │ File: 0-change_your_home_IP
───────┼──────────────────────────────────────────────────────────────────────────────────────────────
   1   │ #!/usr/bin/env bash
   2   │ # Change your home IP
   3   │
   4   │ cp /etc/hosts /tmp/hosts.$$
   5   │ sed 's/^127\.0\.0\.1\(.*localhost\)/127.0.0.2\1/' /tmp/hosts.$$ > /tmp/hosts.$$.tmp
   6   │ sed '/facebook\.com/d' /tmp/hosts.$$.tmp > /tmp/hosts.$$.tmp2
   7   │ echo '8.8.8.8 facebook.com' >> /tmp/hosts.$$.tmp2
   8   │ cp /tmp/hosts.$$.tmp2 /etc/hosts
───────┴──────────────────────────────────────────────────────────────────────────────────────────────
───────┬──────────────────────────────────────────────────────────────────────────────────────────────
       │ File: 0-revert_your_home_IP
───────┼──────────────────────────────────────────────────────────────────────────────────────────────
   1   │ #!/usr/bin/env bash
   2   │ # Revert your home IP
   3   │
   4   │ cp /etc/hosts /tmp/hosts.$$
   5   │ sed 's/^127\.0\.0\.2\(.*localhost\)/127.0.0.1\1/' /tmp/hosts.$$ > /tmp/hosts.$$.tmp
   6   │ sed '/facebook\.com/d' /tmp/hosts.$$.tmp > /tmp/hosts.$$.tmp2
   7   │ cp /tmp/hosts.$$.tmp2 /etc/hosts
───────┴──────────────────────────────────────────────────────────────────────────────────────────────
───────┬──────────────────────────────────────────────────────────────────────────────────────────────
       │ File: 1-show_attached_IPs
───────┼──────────────────────────────────────────────────────────────────────────────────────────────
   1   │ #!/usr/bin/env bash
   2   │ #Write a Bash script that displays all active IPv4 IPs on the machine it's executed on.
   3   │ ifconfig | grep -oP '(?<=inet\s)\d+(\.\d+){3}'
───────┴──────────────────────────────────────────────────────────────────────────────────────────────
───────┬──────────────────────────────────────────────────────────────────────────────────────────────
       │ File: 2-port_listening_on_localhost
───────┼──────────────────────────────────────────────────────────────────────────────────────────────
   1   │ #!/usr/bin/env bash
   2   │ # Write a Bash script that listens on port 98 on localhost.
   3   │ nc -l 127.0.0.1 98
───────┴──────────────────────────────────────────────────────────────────────────────────────────────
